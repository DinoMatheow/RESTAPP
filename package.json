{
  "name": "rest",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/app.ts",
    "build": "rimraf ./dist && tsc",
    "start": "pnpm build && node dist/app.js",
    "test": "pnpm run prisma:migrate:test && jest",
    "test:watch": "pnpm run prisma:migrate:test && jest --watch ",
    "test:coverage": "pnpm run prisma:migrate:test && jest --coverage",
    "prisma:migrate:test": "dotenv -e .env.test -- pnpm prisma migrate deploy"
    
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^11.1.3",
      "onFail": "download"
    }
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/jest": "^30.0.0",
    "@types/node": "^25.9.0",
    "@types/supertest": "^7.2.1",
    "dotenv-cli": "^11.0.0",
    "jest": "^30.4.2",
    "prisma": "^6.19.3",
    "rimraf": "^6.1.3",
    "supertest": "^7.2.2",
    "ts-jest": "^29.4.11",
    "tsx": "^4.22.3",
    "typescript": "^6.0.2"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3",
    "dotenv": "^17.4.2",
    "env-var": "^7.5.0",
    "express": "^5.2.1"
  }
}
